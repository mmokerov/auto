version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: putevoy-list-auto
    ports:
      - "5174:5174"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - HOST=0.0.0.0
    command: npm run dev -- --host 0.0.0.0
