version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: putevoy-list-auto
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=80
    restart: unless-stopped
